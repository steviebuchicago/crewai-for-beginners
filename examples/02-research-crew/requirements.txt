# Verified against crewai 1.15.17 on Python 3.11.
#
# The tools in this example use only the standard library, so nothing is added
# here. Drop "[anthropic]" if you only use OpenAI -- its client is already a
# core crewai dependency.
crewai[anthropic]>=1.15,<2
